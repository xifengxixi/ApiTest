- name: 获取图片验证码
  base_url: ${get_url(yhzx_url)}
  parameters:
  request: 
    method: GET
    url: /user_center/account/imageCode
  # json表达式提取，正则表达式提取
  extract:
    image_code: ${get_image_code}
  validate:
    - equals: {status_code: 200}